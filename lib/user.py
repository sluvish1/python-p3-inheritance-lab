#!/usr/bin/env python

class User:
    # should the knowledge list be put in the user class since both teacher and student need to use it?

    def __init__(self,first_name,last_name):
        self.first_name = first_name
        self.last_name = last_name